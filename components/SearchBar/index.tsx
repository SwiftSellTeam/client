import styled from "styled-components";

const SearchInput = styled.input`
   width: 50%;
`

const SearchBar = () => {
  return (
    <>
        <SearchInput />
    </>
  );
};

export default SearchBar;
